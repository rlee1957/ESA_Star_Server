thresh = 200;
ct = 0;
/*window.setTimeout("zip();", 1000);

function zip()
{
ct++;
var cnt = "";
if(ct < thresh)
	{
	cnt = "<img src='/images/esastar_logo.png' style='position: fixed; top: " + (50 + ct) + "px; left: " + (50 + (ct * 1.1)) + "px; z-index: " + (700 + ct) + "; width: " + (50 + ct) + "px; height: auto;' />";
	bod = document.getElementById("bod");
	bod.innerHTML = bod.innerHTML + cnt;
	window.setTimeout("zoom()", 1000);
	}
}*/